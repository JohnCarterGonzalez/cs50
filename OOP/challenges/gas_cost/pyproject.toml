[tool.poetry]
name = "gas-cost"
version = "0.1.0"
description = ""
authors = ["johncartergonzalez <unmrf0ngh@mozmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pytest = "^7.4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
